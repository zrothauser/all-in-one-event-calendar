{% macro input( id, name='', value = '', type = "text", attributes = [] ) %}
	{% if name == '' %}
		{% set id = name %}
	{% endif %}
	<input 
		type="{{ type }}"
		name="{{ name }}" 
		value="{{ value }}"
		id="{{ id }}"
		{% for attribute, value in attributes %}
			{{ attribute }}="{{ value }}"
		{% endfor %}
		/>
{% endmacro %}