<div class="wrap" style="height:4000px">
    <h2>{{ title }}</h2>
    <div id="poststuff" style="height:4000px">
    <form method="post" action="{{ action }}">
        {{ wp_nonce_field( nonce.action, nonce.name, nonce.referrer ) }}
    <div class="metabox-holder">
    <div class="post-box-container column-1-ai1ec left-side timely" style="height:4000px">
        {{ do_meta_boxes( metabox.screen, metabox.action, metabox.object ) }}
    </div>
    </div>
    </form>
    </div>
</div>
